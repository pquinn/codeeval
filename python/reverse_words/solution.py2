import sys

def main():
    with open(sys.argv[1]) as file:
        for line in file:
            split_reversed = line.strip().split(' ')[::-1]
            print " ".join(split_reversed)

if __name__ == "__main__": main()
